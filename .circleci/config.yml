version: 2.1
setup: true
orbs:
  orb-tools: circleci/orb-tools@12.3
filters: &filters
  tags:
    only: /.*/

workflows:
  lint-pack:
    jobs:
      - orb-tools/lint:
          filters: *filters
      - orb-tools/pack:
          filters: *filters
      - orb-tools/review:
          orb_name: socket
          # RC011 requires usage examples to match the published orb major; patch/minor
          # bumps would otherwise force churn. Examples still track real releases in git.
          exclude: RC011
          filters: *filters
      - orb-tools/continue:
          pipeline_number: << pipeline.number >>
          vcs_type: << pipeline.project.type >>
          orb_name: socket
          requires: [orb-tools/lint, orb-tools/pack, orb-tools/review]
          filters: *filters
